# Email provider: gmail | outlook
EMAIL_PROVIDER=gmail
EMAIL_ACCOUNT=sid.paturkar@gmail.com

# Gmail OAuth — download credentials JSON from Google Cloud Console
# Create OAuth 2.0 Client ID → Desktop application → download JSON
GMAIL_CREDENTIALS_FILE=credentials/gmail_credentials.json
GMAIL_TOKEN_FILE=credentials/gmail_token.json

# Outlook OAuth (Microsoft Graph)
OUTLOOK_CLIENT_ID=
OUTLOOK_TENANT_ID=common
OUTLOOK_CLIENT_SECRET=
OUTLOOK_TOKEN_FILE=credentials/outlook_token.json

# LLM provider: gemini | local
LLM_PROVIDER=gemini

# Google Gemini
GEMINI_API_KEY=AIzaSyAxRT_m04SwM7Pba0lDUg7gh-8Qw1zl_rw
GEMINI_MODEL=gemini-2.5-flash

# Local LLM (Ollama / LM Studio — OpenAI-compatible endpoint)
LOCAL_LLM_BASE_URL=http://localhost:11434/v1
LOCAL_LLM_MODEL=llama3.2:3b
LOCAL_LLM_API_KEY=ollama

# Batch settings
BATCH_SIZE=10
LABEL_PREFIX=AutoSort

# Classification reliability
MAX_CLASSIFY_RETRIES=5
BODY_CHAR_LIMIT=4000
DROPPED_LOG_FILE=dropped_emails.jsonl
